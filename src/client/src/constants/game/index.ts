import { Bones as _Bones } from './bones';
import { VK as _VK } from './keys';
import { GameEvents } from './events';

export const GameConstants = {
    Bones: _Bones,
    VK: _VK,
    Events: GameEvents,
};
